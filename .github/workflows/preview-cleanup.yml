# preview-cleanup.yml
#
# Tears down the preview environment for a deleted branch.
# Triggered automatically when a branch is deleted.
#
# Jobs:
#   cleanup  terraform destroy -> collect remaining previews -> update CDN -> workspace deletion.
#
# Required GitHub environment: preview
#   Secrets: AWS_OIDC_ROLE_ARN, AWS_ACCOUNT_ID
#   Variables: AWS_REGION

name: Preview Cleanup

on:
  delete:
    # Filters are not supported for delete events — we check ref_type in the job.

env:
  PROJECT_NAME: breadly

jobs:
  cleanup:
    name: Cleanup
    if: github.event.ref_type == 'branch' && github.event.ref != 'main'
    runs-on: ubuntu-latest
    environment: preview
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Compute branch slug
        id: slug
        uses: ./.github/actions/slugify-branch
        with:
          branch: ${{ github.event.ref }}

      - name: Setup Terraform (preview module)
        uses: ./.github/actions/terraform-setup
        with:
          working_dir: infrastructure/aws/preview/deploy
          environment: preview
          session_name: github-preview-cleanup-${{ steps.slug.outputs.slug }}
          aws_oidc_role_arn: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws_account_id: ${{ secrets.AWS_ACCOUNT_ID }}
          aws_region: ${{ vars.AWS_REGION }}
          project_name: ${{ env.PROJECT_NAME }}

      - name: Select workspace (skip if not found)
        id: workspace
        working-directory: infrastructure/aws/preview/deploy
        run: |
          SLUG="${{ steps.slug.outputs.slug }}"
          if terraform workspace list | grep -q "preview-${SLUG}"; then
            terraform workspace select "preview-${SLUG}"
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "Workspace preview-${SLUG} not found — nothing to clean up."
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Set Terraform variables
        if: steps.workspace.outputs.exists == 'true'
        run: |
          echo "TF_VAR_aws_account_id=${{ secrets.AWS_ACCOUNT_ID }}" >> "$GITHUB_ENV"
          echo "TF_VAR_aws_region=${{ vars.AWS_REGION }}" >> "$GITHUB_ENV"
          echo "TF_VAR_project_name=${{ env.PROJECT_NAME }}" >> "$GITHUB_ENV"
          echo "TF_VAR_branch_slug=${{ steps.slug.outputs.slug }}" >> "$GITHUB_ENV"
          # Dummy values for variables required by plan/destroy but not needed for destroy
          echo "TF_VAR_dist_zip_path=/dev/null" >> "$GITHUB_ENV"
          echo "TF_VAR_mongodb_uri=placeholder" >> "$GITHUB_ENV"
          echo "TF_VAR_cloudfront_url=https://placeholder.cloudfront.net" >> "$GITHUB_ENV"
          # Create an empty directory for frontend_dist_path (fileset requires a valid directory)
          mkdir -p /tmp/empty-dist
          echo "TF_VAR_frontend_dist_path=/tmp/empty-dist" >> "$GITHUB_ENV"

      - name: Terraform destroy
        if: steps.workspace.outputs.exists == 'true'
        working-directory: infrastructure/aws/preview/deploy
        run: terraform destroy -auto-approve

      # --- Update CDN to remove destroyed preview's origin ---

      - name: Setup Terraform (gateway)
        if: steps.workspace.outputs.exists == 'true'
        uses: ./.github/actions/terraform-setup
        with:
          working_dir: infrastructure/aws/preview/gateway
          environment: preview
          session_name: github-preview-cleanup-gw-${{ steps.slug.outputs.slug }}
          aws_oidc_role_arn: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws_account_id: ${{ secrets.AWS_ACCOUNT_ID }}
          aws_region: ${{ vars.AWS_REGION }}
          project_name: ${{ env.PROJECT_NAME }}

      - name: Get API Gateway endpoint
        if: steps.workspace.outputs.exists == 'true'
        id: gw
        working-directory: infrastructure/aws/preview/gateway
        run: |
          terraform workspace select preview
          echo "api_gateway_url=$(terraform output -raw api_gateway_endpoint)" >> "$GITHUB_OUTPUT"

      - name: Setup Terraform (CDN)
        if: steps.workspace.outputs.exists == 'true'
        uses: ./.github/actions/terraform-setup
        with:
          working_dir: infrastructure/aws/cdn/deploy
          environment: preview
          session_name: github-preview-cleanup-cdn-${{ steps.slug.outputs.slug }}
          aws_oidc_role_arn: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws_account_id: ${{ secrets.AWS_ACCOUNT_ID }}
          aws_region: ${{ vars.AWS_REGION }}
          project_name: ${{ env.PROJECT_NAME }}

      - name: Collect remaining preview buckets
        if: steps.workspace.outputs.exists == 'true'
        id: previews
        working-directory: infrastructure/aws/preview/deploy
        run: |
          PREVIEW_BUCKETS='{}'
          WORKSPACES=$(terraform workspace list | grep -E '^\s+preview-' | sed 's/^[[:space:]]*//' | sed 's/^preview-//' || true)
          for SLUG in $WORKSPACES; do
            terraform workspace select "preview-${SLUG}"
            BUCKET_ID=$(terraform output -raw frontend_bucket_id 2>/dev/null || echo "")
            if [ -n "$BUCKET_ID" ]; then
              BUCKET_ARN=$(terraform output -raw frontend_bucket_arn)
              BUCKET_DOMAIN=$(terraform output -raw frontend_bucket_regional_domain_name)
              PREVIEW_BUCKETS=$(echo "$PREVIEW_BUCKETS" | jq --arg slug "$SLUG" \
                --arg id "$BUCKET_ID" --arg arn "$BUCKET_ARN" --arg domain "$BUCKET_DOMAIN" \
                '. + {($slug): {bucket_id: $id, bucket_arn: $arn, bucket_regional_domain_name: $domain}}')
            fi
          done
          echo "preview_buckets=$PREVIEW_BUCKETS" >> "$GITHUB_OUTPUT"
          echo "Remaining preview buckets: $PREVIEW_BUCKETS"

      - name: Update CDN to remove preview origin
        if: steps.workspace.outputs.exists == 'true'
        uses: ./.github/actions/terraform-action
        with:
          working_dir: infrastructure/aws/cdn/deploy
          environment: preview
          extra_tf_vars: >-
            -var="preview_only=true"
            -var="api_gateway_url=${{ steps.gw.outputs.api_gateway_url }}"
            -var='preview_buckets=${{ steps.previews.outputs.preview_buckets }}'

      - name: Delete Terraform workspace
        if: steps.workspace.outputs.exists == 'true'
        working-directory: infrastructure/aws/preview/deploy
        run: |
          terraform workspace select default
          terraform workspace delete "preview-${{ steps.slug.outputs.slug }}"
