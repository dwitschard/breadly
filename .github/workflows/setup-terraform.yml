# setup-terraform.yml
#
# One-time setup and teardown of the Terraform state backend for the frontend.
# Creates (or destroys) the S3 state bucket and DynamoDB lock table that all
# other frontend Terraform roots depend on.
#
# Jobs
#   setup     Runs terraform plan/apply against the bootstrap root.
#   teardown  Imports and destroys all bootstrap resources.
#
# Triggers
#   workflow_dispatch  Manual trigger only — this workflow is run once per
#                      environment, not as part of the regular deploy pipeline.
#
# Required secrets/variables — see .github/workflows/README.md.
#   Secrets:   AWS_OIDC_ROLE_ARN   AWS_ACCOUNT_ID
#   Variables: AWS_REGION

name: Setup Infrastructure

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        default: dev
        type: choice
        options:
          - dev
          - prod
          - preview
      action:
        description: "Action to perform on the bootstrap stack"
        required: true
        default: create
        type: choice
        options:
          - create
          - destroy

env:
  PROJECT_NAME: breadly

jobs:
  setup:
    name: Create State Backend (${{ inputs.environment }})
    if: inputs.action == 'create'
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Terraform (bootstrap)
        uses: ./.github/actions/terraform-setup
        with:
          working_dir: infrastructure/aws/frontend/setup
          environment: ${{ inputs.environment }}
          session_name: github-setup-frontend-${{ inputs.environment }}
          aws_oidc_role_arn: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws_account_id: ${{ secrets.AWS_ACCOUNT_ID }}
          aws_region: ${{ vars.AWS_REGION }}
          local_backend: "true"
          project_name: ${{ env.PROJECT_NAME }}

      - name: Terraform apply (state backend)
        uses: ./.github/actions/terraform-action
        with:
          working_dir: infrastructure/aws/frontend/setup
          local_backend: "true"

  teardown:
    name: Destroy State Backend (${{ inputs.environment }})
    if: inputs.action == 'destroy'
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    env:
      WORKING_DIR: infrastructure/aws/frontend/setup
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Terraform (bootstrap)
        uses: ./.github/actions/terraform-setup
        with:
          working_dir: infrastructure/aws/frontend/setup
          environment: ${{ inputs.environment }}
          session_name: github-teardown-frontend-${{ inputs.environment }}
          aws_oidc_role_arn: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws_account_id: ${{ secrets.AWS_ACCOUNT_ID }}
          aws_region: ${{ vars.AWS_REGION }}
          local_backend: "true"
          project_name: ${{ env.PROJECT_NAME }}

      - name: Import existing state resources
        working-directory: ${{ env.WORKING_DIR }}
        shell: bash
        run: |
          terraform import aws_s3_bucket.tfstate ${{ env.PROJECT_NAME }}-${{ inputs.environment }}-tfstate
          terraform import aws_dynamodb_table.tfstate_lock ${{ env.PROJECT_NAME }}-${{ inputs.environment }}-tfstate-lock

      - name: Terraform destroy (state backend)
        working-directory: ${{ env.WORKING_DIR }}
        shell: bash
        run: terraform destroy -auto-approve
