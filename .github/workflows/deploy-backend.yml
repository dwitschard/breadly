# deploy-backend.yml
#
# Deployment pipeline for the Breadly backend.
# Deploys the Express API as an AWS Lambda function via Terraform.
#
# Jobs
#   deploy    Downloads the release asset and deploys via Terraform.
#
# Triggers
#   workflow_call     Called by build-backend.yml on main to auto-deploy to dev.
#   workflow_dispatch Manually deploy a specific release to a target environment.
#
# Prerequisites
#   The Terraform state backend (S3 bucket + DynamoDB table) must already exist.
#   Run setup-deployment-infrastructure.yml once per environment before the first deploy.
#
# Required secrets/variables — see .github/workflows/README.md.
#   Secrets:   AWS_OIDC_ROLE_ARN   AWS_ACCOUNT_ID   MONGODB_URI
#   Variables: AWS_REGION

name: Deploy Backend

on:
  workflow_call:
    inputs:
      release_tag:
        required: true
        type: string
      environment:
        required: true
        type: string

  workflow_dispatch:
    inputs:
      release_tag:
        description: "Release tag to deploy (e.g. backend-a1b2c3d)"
        required: true
        type: string
      environment:
        description: "Target environment"
        required: true
        default: dev
        type: choice
        options:
          - dev
          - prod

concurrency:
  group: deploy-backend-${{ github.ref }}
  cancel-in-progress: true

env:
  TARGET_ENV: ${{ inputs.environment }}
  PROJECT_NAME: breadly

jobs:
  deploy:
    name: ${{ inputs.environment }}
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Download release asset
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release download ${{ inputs.release_tag }} --pattern backend.zip

      - name: Setup Terraform
        uses: ./.github/actions/terraform-setup
        with:
          working_dir: infrastructure/aws/backend/deploy
          environment: ${{ env.TARGET_ENV }}
          session_name: github-deploy-backend-${{ env.TARGET_ENV }}
          aws_oidc_role_arn: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws_account_id: ${{ secrets.AWS_ACCOUNT_ID }}
          aws_region: ${{ vars.AWS_REGION }}
          project_name: ${{ env.PROJECT_NAME }}

      - name: Setup Terraform (CDN — read-only)
        uses: ./.github/actions/terraform-setup
        with:
          working_dir: infrastructure/aws/cdn/deploy
          environment: ${{ env.TARGET_ENV }}
          session_name: github-deploy-backend-cdn-${{ env.TARGET_ENV }}
          aws_oidc_role_arn: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws_account_id: ${{ secrets.AWS_ACCOUNT_ID }}
          aws_region: ${{ vars.AWS_REGION }}
          project_name: ${{ env.PROJECT_NAME }}

      - name: Get frontend URL from CDN stack
        id: frontend
        working-directory: infrastructure/aws/cdn/deploy
        run: |
          CF_URL=$(terraform output -raw cloudfront_url)
          if [ "${{ env.TARGET_ENV }}" = "dev" ]; then
            echo "urls=http://localhost:4200,${CF_URL}" >> "$GITHUB_OUTPUT"
          else
            echo "urls=${CF_URL}" >> "$GITHUB_OUTPUT"
          fi

      - name: Export Terraform secrets
        run: echo "TF_VAR_mongodb_uri=${{ secrets.MONGODB_URI }}" >> "$GITHUB_ENV"

      - name: Deploy stack
        uses: ./.github/actions/terraform-action
        with:
          working_dir: infrastructure/aws/backend/deploy
          environment: ${{ env.TARGET_ENV }}
          extra_tf_vars: >-
            -var="dist_zip_path=${{ github.workspace }}/backend.zip"
            -var="frontend_urls=${{ steps.frontend.outputs.urls }}"

      - name: Print API Gateway endpoint
        working-directory: infrastructure/aws/backend/deploy
        run: terraform output -raw api_gateway_endpoint
