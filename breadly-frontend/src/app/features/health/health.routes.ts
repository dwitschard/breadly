import { Routes } from '@angular/router';
import { HealthContainerComponent } from './containers/health.container';

export const HEALTH_ROUTES: Routes = [{ path: '', component: HealthContainerComponent }];
