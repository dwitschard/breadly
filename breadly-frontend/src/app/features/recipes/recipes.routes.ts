import { Routes } from '@angular/router';
import { RecipesListContainerComponent } from './containers/recipes-list.container';

export const RECIPES_ROUTES: Routes = [{ path: '', component: RecipesListContainerComponent }];
